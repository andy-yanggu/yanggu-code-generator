/**
 * 布局相关配置
 */

/**
 * 菜单展开宽度约束（像素）
 */
export const MENU_EXPAND_WIDTH = {
	min: 160,
	max: 300,
	default: 210
} as const

/**
 * 菜单折叠宽度约束（像素）
 */
export const MENU_FOLD_WIDTH = {
	min: 40,
	max: 80,
	default: 60
} as const
