<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yanggu.code.generator.mapper.FieldTypeMapper">

    <!-- 所有字段 -->
    <sql id="allColumns">
        id,
        column_type,
        attr_type,
        package_name,
        create_time,
        update_time,
        is_delete
    </sql>

    <!-- 过滤SQL -->
    <sql id="whereSQL">
        <!-- 这里写过滤语句，过滤项需要加query前缀 -->
        <if test="query.columnType != null and query.columnType.trim() != ''">
            AND column_type LIKE CONCAT('%', #{query.columnType}, '%')
        </if>
        <if test="query.attrType != null and query.attrType.trim() != ''">
            AND attr_type LIKE CONCAT('%', #{query.attrType}, '%')
        </if>
    </sql>

    <!-- Entity类字段映射 -->
    <resultMap type="com.yanggu.code.generator.domain.entity.FieldTypeEntity" id="fieldTypeMap">
        <id property="id" column="id"/>
        <result property="columnType" column="column_type"/>
        <result property="attrType" column="attr_type"/>
        <result property="packageName" column="package_name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="isDelete" column="is_delete"/>
    </resultMap>

    <!-- Entity分页 -->
    <select id="entityPage" resultMap="fieldTypeMap">
        SELECT
            <include refid="allColumns"/>
        FROM
            gen_field_type
        <where>
            is_delete = 0
            <include refid="whereSQL"/>
        </where>
        <include refid="com.yanggu.code.generator.common.mybatis.mapper.BaseMapperPlus.orderBySQL"/>
    </select>

    <!-- Entity列表 -->
    <select id="entityList" resultMap="fieldTypeMap">
        SELECT
            <include refid="allColumns"/>
        FROM
            gen_field_type
        <where>
            is_delete = 0
            <include refid="whereSQL"/>
        </where>
        <include refid="com.yanggu.code.generator.common.mybatis.mapper.BaseMapperPlus.orderBySQL"/>
    </select>

    <!-- VO分页 -->
    <select id="voPage" resultType="com.yanggu.code.generator.domain.vo.FieldTypeVO">
        SELECT
            <include refid="allColumns"/>
        FROM
            gen_field_type
        <where>
            is_delete = 0
            <include refid="whereSQL"/>
        </where>
        <include refid="com.yanggu.code.generator.common.mybatis.mapper.BaseMapperPlus.orderBySQL"/>
    </select>

    <!-- VO列表 -->
    <select id="voList" resultType="com.yanggu.code.generator.domain.vo.FieldTypeVO">
        SELECT
            <include refid="allColumns"/>
        FROM
            gen_field_type
        <where>
            is_delete = 0
            <include refid="whereSQL"/>
        </where>
        <include refid="com.yanggu.code.generator.common.mybatis.mapper.BaseMapperPlus.orderBySQL"/>
    </select>

    <select id="getPackageByTableId" resultType="String">
        select t1.package_name
        from gen_field_type t1,
             gen_table_field t2
        where t1.attr_type = t2.attr_type
          and t2.table_id = #{value}
    </select>

    <select id="distinctAttrTypeList" resultType="java.lang.String">
        SELECT
            attr_type
        FROM
            gen_field_type
        WHERE
            is_delete = 0
        GROUP BY
            attr_type
    </select>

</mapper>