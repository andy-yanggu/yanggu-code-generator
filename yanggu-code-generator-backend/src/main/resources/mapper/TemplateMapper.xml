<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yanggu.code.generator.mapper.TemplateMapper">

    <!-- 所有字段 -->
    <sql id="allColumns">
        id,
        template_group_id,
        template_name,
        generator_path,
        template_desc,
        template_content,
        template_type,
        create_time,
        update_time,
        is_delete
    </sql>

    <!-- 过滤SQL -->
    <sql id="whereSQL">
        <!-- 这里写过滤语句，过滤项需要加query前缀 -->
        <if test="query.templateGroupId != null">
            AND template_group_id = #{query.templateGroupId}
        </if>
        <if test="query.templateName != null and query.templateName.trim() != ''">
            AND template_name LIKE CONCAT('%', #{query.templateName}, '%')
        </if>
        <if test="query.templateType != null">
            AND template_type = #{query.templateType}
        </if>
    </sql>

    <!-- 排序SQL -->
    <sql id="orderBySQL">
        <if test="query.orders != null and query.orders.size() > 0">
            ORDER BY
            <foreach collection="query.orders" item="order" separator=",">
                ${order.column} <if test="order.isAsc">ASC</if><if test="!order.isAsc">DESC</if>
            </foreach>
        </if>
    </sql>

    <!-- Entity类字段映射 -->
    <resultMap type="com.yanggu.code.generator.domain.entity.TemplateEntity" id="templateMap">
        <id property="id" column="id"/>
        <result property="templateGroupId" column="template_group_id"/>
        <result property="templateName" column="template_name"/>
        <result property="generatorPath" column="generator_path"/>
        <result property="templateDesc" column="template_desc"/>
        <result property="templateContent" column="template_content"/>
        <result property="templateType" column="template_type"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="isDelete" column="is_delete"/>
    </resultMap>

    <!-- Entity分页 -->
    <select id="entityPage" resultMap="templateMap">
        SELECT
            <include refid="allColumns"/>
        FROM
            yanggu_code_generator.gen_template
        <where>
            is_delete = 0
            <include refid="whereSQL"/>
        </where>
        <include refid="orderBySQL"/>
    </select>

    <!-- Entity列表 -->
    <select id="entityList" resultMap="templateMap">
        SELECT
            <include refid="allColumns"/>
        FROM
            yanggu_code_generator.gen_template
        <where>
            is_delete = 0
            <include refid="whereSQL"/>
        </where>
        <include refid="orderBySQL"/>
    </select>

    <!-- VO分页 -->
    <select id="voPage" resultType="com.yanggu.code.generator.domain.vo.TemplateVO">
        SELECT
            <include refid="allColumns"/>
        FROM
            yanggu_code_generator.gen_template
        <where>
            is_delete = 0
            <include refid="whereSQL"/>
        </where>
        <include refid="orderBySQL"/>
    </select>

    <!-- VO列表 -->
    <select id="voList" resultType="com.yanggu.code.generator.domain.vo.TemplateVO">
        SELECT
            <include refid="allColumns"/>
        FROM
            yanggu_code_generator.gen_template
        <where>
            is_delete = 0
            <include refid="whereSQL"/>
        </where>
        <include refid="orderBySQL"/>
    </select>

</mapper>